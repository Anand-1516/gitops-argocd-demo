apiVersion: apps/v1
kind: Deployment
metadata:
  name: gitops-demo
spec:
  template:
    spec:
      containers:
      - name: app
        image: anand1516/gitops-demo:0.1.0

        env:
        - name: APP_VERSION
          value: "v0.1.0"
